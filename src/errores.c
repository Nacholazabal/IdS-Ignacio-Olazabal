#include "errores.h"

void registrar_error(gravedad_t gravedad, const char* funcion, int linea, const char* mensaje)
{
    // Implementación vacía por ahora - no hace nada
    // TODO: Implementar logging real cuando sea necesario
    (void) gravedad;  // Evitar warnings de parámetros no usados
    (void) funcion;
    (void) linea;
    (void) mensaje;
}
